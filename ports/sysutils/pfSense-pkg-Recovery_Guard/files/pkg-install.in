#!/bin/sh

# Native pfSense package registration, only after files are installed.
if [ "$2" != "POST-INSTALL" ]; then
	exit 0
fi

exec "${PKG_ROOTDIR}/usr/local/bin/php" -f "${PKG_ROOTDIR}/etc/rc.packages" "%%PORTNAME%%" "$2"
